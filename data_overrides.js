/* Manual label nudges, keyed by label (or "PROTEIN:label").
   dx = pixels right(+)/left(-);  lane = stack level (0 = shortest line). */
window.VARIANT_OVERRIDES = {
  "Y72C": { "lane": 0, "dx": 14 },
  "R70C": { "dx": -14 }
};
